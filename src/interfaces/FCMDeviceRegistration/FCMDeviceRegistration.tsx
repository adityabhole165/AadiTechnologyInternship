import { DeviceId, DeviceInfo } from "@capacitor/device";

export interface IPushNotificationFCM{
    asSchoolId: string,
    asUserId: string,
    asRegistrationId: string,
    asDeviceId: string,
    asDeviceType: string
}